import StudentDatabase from '../StudentDatabase';

export default function StudentDatabaseExample() {
  return (
    <div className="min-h-screen bg-background">
      <StudentDatabase />
    </div>
  );
}